// Merge arrays means that you will be given two arrays and you will merge them into one array using the concat method.

let array1 = [1,2,3]
let array2 = [4,5,6]

function mergeArrays(array1, array2) {
    return array1.concat(array2);

}

console.log(mergeArrays(array1,array2))